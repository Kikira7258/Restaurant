<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Document</title>
</head>
<body class="mx-24 my-8 bg-gradient-to-r from-cyan-200 to to-green-200">


    <h1 class="text-[50px] mb-8 text-center">Menu</h1>

<ul class="grid grid-cols-4 gap-8 gap-y-16">
    <% if(data.length) {
        for(var i = 0; i < data.length; i++) { %>
        
            <li class="bg-white w-fit px-[0.8rem] py-2 rounded-md shadow-md shadow-slate-500 ">
                <h2 class="text-[20px] mb-2"><%= data[i].item_name %></h2>

                <div class="w-[150px] h-[100px] bg-blue-300 mx-auto overflow-hidden">
                    <img class="object-cover" src="<%= data[i].img_url %>">
                </div>

                <p class="my-2"><%= data[i].item_description %></p>

                <div class="flex justify-between">
                    <p>$<%= data[i].item_price %></p>

                    <!-- step 1 -->
                    <a href="/create-order/<%= data[i].id %>">
                        <button class="bg-green-200 rounded-md px-[10px] py-[3px] shadow-md shadow-slate-500/50 hover:">Buy</button>
                    </a>
                </div>

            </li>
        <% }

    } else { %>
    <li>No Records</li>
<% } %>
    
</ul>
    
</body>
</html>


